---
title: Responses
description: Common Body Responses.
icon: pixelarticons:menu-square
---

## SingleResourceResponse

Wrapper used when an endpoint returns one resource in `data`.

```ts
interface SingleResourceResponse<T> {
  data: T;
}
```

## ListResourceResponse

Wrapper used when an endpoint returns a non-paginated list in `data`.

```ts
interface ListResourceResponse<T> {
  data: T[];
}
```

## PaginatedResourceResponse

Wrapper used for paginated list endpoints, including paging metadata.

```ts
interface PaginatedResourceResponse<T> {
  data: T[];
  page: {
    limit: number;
    offset: number;
    has_next_page: boolean;
  };
}
```

## ErrorResponse

Standard error envelope returned by OpenMusic endpoints.

```ts
interface ErrorResponse {
  error: {
    code: number;
    title: string;
    message: string;
    trace_id: string | null;
  };
}
```
